// ===== SCROLL REVEALS =====
    // Purpose: Adds restrained entrance motion to supporting content.
    // Triggers: Elements animate once as they enter the viewport.
    AOS.init({ once: true, duration: 700, offset: 80 });